# Local upload staging — raw writeups before they're processed into writeups/
htb writeups for upload/
htb writeups for upload.zip
*.zip
.DS_Store

# Cloudflare Worker local state / secrets — never commit
worker/.wrangler/
worker/.dev.vars
.dev.vars
__pycache__/
*.pyc
